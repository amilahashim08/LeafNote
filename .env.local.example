# URL of the API used by next.config rewrites (server-side only).
# Default is http://127.0.0.1:4000 if unset.
BACKEND_URL=http://127.0.0.1:4000

# Optional public URL of this app (for auth if you add next-auth, etc.)
NEXTAUTH_URL=http://localhost:3000
